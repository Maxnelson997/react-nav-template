import React, { Component } from 'react'

class HomeCard extends Component {
   render() {
       const { className } = this.props
       return (
           <div className={`${className} home-card`}>
                
           </div>
       )
   }
}

export default HomeCard;