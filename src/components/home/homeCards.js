import React, { Component } from 'react'

class HomeCards extends Component {
   render() {
       const { className } = this.props
       return (
           <div className={`${className} home-cards`}>
            
           </div>
       )
   }
}

export default HomeCards;